apiVersion: v1
kind: ConfigMap
metadata:
  name: taskmaster-config
  namespace: taskmaster-pro
data:
  NODE_ENV: "production"
  PORT: "3001"
  JWT_EXPIRES_IN: "7d"
  CORS_ORIGIN: "http://localhost:3000"
  # Database config will come from secrets
  # Redis config will come from secrets
